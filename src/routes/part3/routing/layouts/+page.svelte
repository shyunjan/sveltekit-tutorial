<div class="tutorial">
  <p>
    A <code>+layout.svelte</code> file(<code>src\routes\+layout.svelte</code>) applies to every child route, including the sibling
    <code>+page.svelte</code> (if it exists). You can nest layouts to arbitrary depth.
  </p>
</div>
